# .env.example

# 1. MongoDB Connection URI (Crucial for DB and GridFS)
# Replace <username>, <password>, and the cluster details with your actual credentials.
# If using MongoDB Atlas, get the connection string from your cluster setup.
MONGO_URI="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/vcs_db"

# 2. Server Port
# This is where your Express server will listen.
PORT=3000

# 3. JWT Secret (Recommended for future authentication)
# Use a long, complex string for security.
JWT_SECRET="YOUR_SUPER_SECURE_RANDOM_SECRET_KEY_HERE"